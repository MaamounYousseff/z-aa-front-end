class NotUnderstand {
  final String message;
  const NotUnderstand(this.message);
}

class NotImplemented {
  final String message;
  const NotImplemented(this.message);
}

class NotCompleted {
  final String message;
  const NotCompleted(this.message);
}

class NotTested {
  final String message;
  const NotTested(this.message);
}

class Answer {
  final String message;
  const Answer(this.message);
}
